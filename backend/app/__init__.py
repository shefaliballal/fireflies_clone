"""
Fireflies.ai clone — FastAPI backend application package.

This package holds the API, database layer, and (later) domain models for
meeting transcription and note-taking. Import the app via `app.main:app`
when running under uvicorn.
"""
